spring:
  main:
    banner-mode: off
  redis:
    database: 0
    host: localhost
    password: ""
    port: 6379
    client-name: "redis-connector"
    timeout: 60000
    num-tests-per-eviction-run: 3
    max-total: 100
    max-wait: 60000
    max-idle: 10
    max-active: 1
    min-idle: 5
    max-wait-millis: 1000
    command-timeout-millis: 2000
    connection-timeout-millis: 60000
    time-between-eviction-runs: 60000
